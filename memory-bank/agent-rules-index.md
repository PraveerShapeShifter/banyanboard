# Agent Rules Index

> Auto-generated by `/banyan-rules-index`. Maps user/learned rule files to the file patterns, paths, and topics that trigger them so sub-agents can load only what's relevant. Last generated: 2026-07-13.

## Summary

- **Total rule files**: 6 (all auto-generated `_learned/`, none hand-authored)
- **Cap**: 10 learned files (6 used)
- **Conflicts detected**: none
- **Unsafe rules rejected**: none

## Rules

| Rule | File | Priority | globs | topics |
|------|------|----------|-------|--------|
| Learned: Testing Patterns | `agent-rules/_learned/testing-patterns.md` | medium | `src/**/*.ts`, `**/*.test.ts` | testing, dependency-injection |
| Learned: Error Handling | `agent-rules/_learned/error-handling.md` | low | `src/**/*.ts`, `src/**/health*`, `src/app.ts` | error-handling, observability |
| Learned: API Design | `agent-rules/_learned/api-design.md` | low | `src/**/*.routes.ts` | api-design, rest |
| Learned: Data Access | `agent-rules/_learned/data-access.md` | low | `src/**/*.repository.ts` | data-access, sql, security |
| Learned: Configuration | `agent-rules/_learned/configuration.md` | low | `src/config/**`, `docker-compose*.yml` | configuration, 12-factor |
| Learned: Infrastructure | `agent-rules/_learned/infrastructure.md` | low | `Dockerfile`, `docker-compose*.yml`, `.dockerignore` | infrastructure, docker, verification |

## Topic → Rules

- **testing / dependency-injection** → Testing Patterns
- **error-handling / observability** → Error Handling
- **api-design / rest** → API Design
- **data-access / sql / security** → Data Access
- **configuration / 12-factor** → Configuration
- **infrastructure / docker** → Infrastructure

## Notes for the frontend (`frontend/**`, TASK-004)

All six learned rules were extracted from the backend (TASK-001/002/003) and are scoped to backend paths (`src/**`, Docker, compose). The **one transferable principle** for the new React frontend is from **Testing Patterns**: inject external I/O as a dependency and stub it in tests. For the frontend this means the **API client is the single injectable I/O seam** — component tests stub the client, never a live backend (already ratified in the TASK-004 Architecture creative). The 12-factor **Configuration** principle also applies: the API base URL comes from env (`import.meta.env.VITE_API_BASE_URL`), never hardcoded.
