# Agent Rules Index

> Auto-generated by `/banyan-rules-index`. Maps user/learned rule files to the file patterns, paths, and topics that trigger them so sub-agents can load only what's relevant. Last generated: 2026-07-14.

## Summary

- **Total rule files**: 7 (all auto-generated `_learned/`, none hand-authored)
- **Cap**: 10 learned files (7 used)
- **Conflicts detected**: none
- **Unsafe rules rejected**: none

## Rules

| Rule | File | Priority | globs | topics |
|------|------|----------|-------|--------|
| Learned: Testing Patterns | `agent-rules/_learned/testing-patterns.md` | medium | `src/**/*.ts`, `**/*.test.ts`, `**/*.test.tsx` | testing, dependency-injection |
| Learned: Error Handling | `agent-rules/_learned/error-handling.md` | medium | `src/**/*.ts`, `src/**/health*`, `src/app.ts`, `frontend/src/pages/**/*.tsx` | error-handling, observability, async-ui |
| Learned: Frontend Patterns | `agent-rules/_learned/frontend-patterns.md` | low | `frontend/src/**/*.ts`, `frontend/src/**/*.tsx` | frontend, react, async-ui |
| Learned: API Design | `agent-rules/_learned/api-design.md` | low | `src/**/*.routes.ts` | api-design, rest |
| Learned: Data Access | `agent-rules/_learned/data-access.md` | low | `src/**/*.repository.ts` | data-access, sql, security |
| Learned: Configuration | `agent-rules/_learned/configuration.md` | low | `src/config/**`, `docker-compose*.yml`, `**/vite.config.*`, `frontend/package.json` | configuration, 12-factor, build-tooling |
| Learned: Infrastructure | `agent-rules/_learned/infrastructure.md` | low | `Dockerfile`, `docker-compose*.yml`, `.dockerignore` | infrastructure, docker, verification |

## Topic → Rules

- **testing / dependency-injection** → Testing Patterns
- **error-handling / observability / async-ui** → Error Handling
- **frontend / react / async-ui** → Frontend Patterns
- **api-design / rest** → API Design
- **data-access / sql / security** → Data Access
- **configuration / 12-factor / build-tooling** → Configuration
- **infrastructure / docker** → Infrastructure

## Notes for the frontend (`frontend/**`)

As of TASK-004, three rules now carry frontend-applicable guidance:
- **Frontend Patterns** (new) — confine all `fetch` to a single client module returning a discriminated result; model each page's async state as one status union so "never render stale/blank while pending" is structural.
- **Testing Patterns** — the API client is the single injectable I/O seam; component tests stub the client (never a live backend) and query by ARIA role/accessible name (`*.test.tsx` now in scope).
- **Error Handling** — in async data-driven UI, branch on a specific failure status (e.g. 404 = not-found) before falling through to a generic error state; give it its own component/copy and no misleading Retry.
- **Configuration** — API base URL from env (`import.meta.env.VITE_API_BASE_URL`), never hardcoded; keep a test runner and its host bundler (Vitest/Vite) on the same major version.

The remaining rules (API Design, Data Access, Infrastructure) stay backend-scoped and do not apply to the read-only frontend.
