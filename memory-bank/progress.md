# Progress Log

Chronological record of completed phases and tasks. Updated by `/banyan-archive`.

---

- **2026-07-15** — TASK-005 (Realtime Activity Feed, FEAT-005) planned via `/banyan-plan` → PLANNING_COMPLETE (Level 4, auto-provisioned for FEAT-005). Spec Writer (Opus) produced an End-User Feature spec with 10 ACs (1 ENTRY / 2 HAPPY / 2 ASYNC / 1 ERROR / 3 VERIFY / 1 INTEGRATION); taxonomy lint CLEAN; human approved. Plan: 4 stages (Phase 1 activity model + event capture on the `PATCH /cards/:id` path → `003_*` table + injected in-process emitter; Phase 2 realtime push transport + backfill/replay; Phase 3 frontend live feed on the board view; Phase 4 post-UAT E2E), ~26 tests (backend integration + frontend component + 1 journey), Observability=Yes (structured logging via existing `log()`), REST API = 1 new push-transport endpoint + additive `003_*` schema. Five LOW-confidence questions drive **2 REQUIRED creative phases**: Architecture Design (SSE-vs-WS transport, event-capture old≠new detection, activity/event model + backfill cursor, per-board fan-out + single→multi-instance boundary) and UI/UX Design (feed surface/placement, states, WCAG 2.1 AA `aria-live`/non-color/SR phrasing). Key scope calls: read-only feed; no auth → generic actor ("someone"); multi-instance transport scaling out of MVP (boundary documented, not built). Next: `/banyan-creative TASK-005`.

- **2026-07-15** — TASK-005 creative complete (`/banyan-creative`) → CREATIVE_COMPLETE. Two phases run sequentially (transport decision informs the feed's state model). **Architecture Design** (Opus, `creative/TASK-005-realtime-activity-feed-architecture.md`) — **SSE** (`GET /api/activity/stream?board_id=`, traverses the existing Vite `/api` proxy unchanged, `src/server.ts` stays on `app.listen`, native `Last-Event-ID` reconnect); **capture via a `RETURNING` CTE in `cards.repository.update()`** orchestrated in the `PATCH /cards/:id` handler (persist-first, emit off the response path, gated on `previousStatus !== newStatus` → no-op/non-status PATCH emits nothing); new **`card_activity`** table (identity PK = cursor, `board_id` CASCADE, `card_id` SET NULL, denormalized `card_title`, CHECK statuses, `(board_id, id)` index); **per-board in-process `ActivityEmitter`** injected via `createApp(deps)` (`AppDeps` += `activityRepo`+`activityEmitter`), subscribe-before-backfill dedupe, heartbeat + `req.on('close')` cleanup; single→multi-instance boundary (LISTEN/NOTIFY/Redis) documented behind the emitter interface, not built. **UI/UX Design** (Sonnet, `creative/TASK-005-realtime-activity-feed-uiux.md`) — **persistent side panel** (desktop 4th grid track, tablet full-width row below, mobile 4th stacked section after Done); phrasing `{actor} moved "{title}" from {From} to {To}` with actor="Someone" (no auth), deleted cards render identically via denormalized title, absolute `<time>` stamps; five states (connecting=reuse `Loading`, open+streaming, open+empty=reuse `EmptyState`, reconnecting=`ActivityFeedStatus` `role=status`, degraded=`role=alert` with list frozen); a11y = `aria-live="polite"` on a **visually-hidden dedicated announcer** with an "arming heuristic" so backfill/replay bursts stay silent (kills chatty-feed risk); components `ActivityFeed`/`ActivityFeedItem`/`ActivityFeedStatus`/`useActivityStream`/`api/activityStream.ts`. Next: `/banyan-build TASK-005` (Phase 1: activity model + event capture).

- **2026-07-15** — TASK-005 (Realtime Activity Feed, FEAT-005) Phase 1/3 complete: **Activity model + event capture** (transport-agnostic backend). Added `db/init/003_card_activity.sql` (identity PK doubling as the SSE cursor; `board_id` FK `ON DELETE CASCADE`; `card_id` FK `ON DELETE SET NULL`; denormalized `card_title` so history survives card deletion; `from_status`/`to_status` CHECK-constrained to the `CardStatus` enum; `created_at TIMESTAMPTZ`; composite `card_activity_board_id_id_idx (board_id, id)` for backfill+replay). New `src/activity/` module: `activity.types.ts`, `activity.repository.ts` (`ActivityRepository` + `PostgresActivityRepository` — parameterized `record`/`findRecentByBoard`/`findAfter`), `activity.emitter.ts` (`ActivityEmitter` + `InProcessActivityEmitter`, per-board `Map<boardId,Set<handler>>`, per-handler isolation on fan-out). Capture wired into `PATCH /cards/:id`: `cardsRepo.update()` now returns `{ card, previousStatus }` from a single atomic `RETURNING` subquery (race-free old-vs-new); the handler persists exactly one `card_activity` row **then** emits, only on a real transition (`previousStatus !== card.status`), fail-safe (capture failure logged via `activity.capture.error`, never fails/alters the PATCH response). `AppDeps` + `server.ts` inject `activityRepo`+`activityEmitter`. **Verification**: 105/105 tests PASS (was 95; +9 capture/repo, +1 fail-safe); `tsc` build clean; lint N/A. Code review (build-code-reviewer-agent): APPROVE-WITH-FIXES, 0 blocking — applied emitter per-handler try/catch + snapshot, aligned index name, added the AC-VERIFY-3 fail-safe test. **Delivers AC-VERIFY-1/2/3 + persistence half of AC-INTEGRATION-1.** Committed to `feature/FEAT-005-realtime-activity-feed`. Next: `/banyan-build TASK-005` for Phase 2 (SSE transport + backfill/replay).

- **2026-07-15** — TASK-005 (Realtime Activity Feed, FEAT-005) Phase 2/3 complete: **Realtime push transport (SSE) + backfill/replay** (backend). Added `src/activity/activity.routes.ts`: `GET /activity/stream?board_id=` → `text/event-stream`. Per connection: validate `board_id`→400; SSE headers + `flushHeaders()`; **subscribe to the emitter before the backfill read** (buffer live events during the read, flush deduped by `id` cursor — no gap/no dup); `Last-Event-ID` header → `findAfter` replay, else `findRecentByBoard` backfill (both bounded by `ACTIVITY_BACKFILL_LIMIT`); one `card_moved` frame per event oldest→newest; `: keep-alive` heartbeat (`ACTIVITY_HEARTBEAT_MS`); idempotent teardown on `req` close. `activityStreamHandler` exported separately from `createActivityRouter` for deterministic mock-req/res testing. Wired via `env.ts`/`app.ts`/`server.ts`/`docker-compose.yml` (12-factor). **Code review found 2 real blocking bugs the initial tests missed** — (1) `req.on('close')` was registered *after* the backfill await, so a disconnect mid-read leaked the subscription + heartbeat and risked a write-after-close process crash; (2) a malformed `Last-Event-ID` coerced to `NaN`, silently dropping every buffered live event. **Both fixed**: cleanup registered before the await with an idempotent `closed` flag guarding all writes (+ no heartbeat if already closed); `parseCursor()` validates the header and falls back to full backfill on malformed input. Added 3 regression tests for exactly these scenarios. **Verification**: 118/118 tests PASS (was 115; +3 regression); `tsc` build clean; lint N/A. **Delivers server side of AC-HAPPY-1, AC-ASYNC-1/2, AC-ERROR-1, AC-VERIFY-3.** Committed to `feature/FEAT-005-realtime-activity-feed`. Next: `/banyan-build TASK-005` for Phase 3 (frontend live feed UI — completes the entry→success journey).

- **2026-07-10** — Memory Bank initialized via `/banyan-init` (greenfield).
- **2026-07-10** — FEAT-001 (Project Foundation) added to roadmap; TASK-001 planned via `/banyan-plan` → PLANNING_COMPLETE (Level 2, 3 build phases).
- **2026-07-10** — TASK-001 Phase 1/3 complete: TS/Express skeleton, env config, structured logger, project structure, Vitest+Supertest smoke tests (2/2 pass), README. Build + tests green. Committed to `feature/FEAT-001-project-foundation`.
- **2026-07-10** — TASK-001 Phase 2/3 complete: `GET /health` with PostgreSQL connectivity check (200 `{status:ok,db:connected}` / 503 `{status:degraded,db:disconnected}`, never crashes). `pg` pool isolated in `src/db/pool.ts`; `checkDb` dependency injected into `createApp` for testability. Added 5 health tests (7/7 total pass). Build green. README API docs added. AC-HAPPY-1/2 and AC-ERROR-1 satisfied. Committed to `feature/FEAT-001-project-foundation`.
- **2026-07-10** — TASK-001 Phase 3/3 complete (final phase): Docker Compose orchestration. Added multi-stage `Dockerfile` (node:20-alpine build → slim non-root runtime), `docker-compose.yml` (api + postgres:16-alpine with healthchecks, named volume, `api depends_on db: service_healthy`, `DATABASE_URL` wired to `db:5432`), and `.dockerignore`. README Docker Quick Start added. `npm run build` PASS, `npm test` 7/7 PASS (no regression), `docker compose config` VALID. Live `docker compose up` deferred to human (Docker daemon unavailable in build env) — per Test Strategy Phase 3 has 0 automated tests, Docker verified manually via AC-ENTRY-1. TASK-001 now BUILD_COMPLETE. Committed to `feature/FEAT-001-project-foundation`. Next: `/banyan-reflect TASK-001`.
- **2026-07-10** — TASK-001 reflection complete (`/banyan-reflect`). Reflection doc `memory-bank/reflection/reflection-TASK-001.md`. Ratings: Task Quality High, Ecosystem Effectiveness Good. Continuous learning: 4 patterns extracted → 4 new `_learned/` rules created (testing-patterns, error-handling, infrastructure, configuration). Status → REFLECTION_COMPLETE. Open follow-up: human to run `docker compose up --build` + `curl /health` to live-confirm AC-ENTRY-1. Next: `/banyan-archive TASK-001` (optional for Level 2).
- **2026-07-12** — TASK-002 (Board CRUD API) Phase 1/2 complete: **Data layer**. Added `boards` schema via committed SQL init script `db/init/001_boards.sql` (INTEGER identity PK, `name VARCHAR(120)`, `description TEXT`, `created_at`/`updated_at TIMESTAMPTZ`) mounted into the `db` compose service at `/docker-entrypoint-initdb.d/`. Added `src/boards/boards.repository.ts` — `BoardsRepository` interface (DI, mirrors `DbHealthCheck`) + `PostgresBoardsRepository` over the existing `pg` pool: parameterized queries (injection-safe), `RETURNING`-based create/update, dynamic partial `update` that bumps `updated_at`, hard `delete` reporting `rowCount`. 12 unit tests in `boards.repository.test.ts` (mocked pool). `npm test` 19/19 PASS (12 new + 7 existing, no regression), `npm run build` (tsc) PASS, lint N/A (no lint script). Committed to `feature/FEAT-002-board-crud`. Next: `/banyan-build TASK-002` for Phase 2 (HTTP layer: validation, routes, app/server wiring).
- **2026-07-12** — TASK-002 (Board CRUD API) Phase 2/2 complete (final phase): **HTTP layer**. Added `src/boards/boards.validation.ts` (hand-rolled create/update validators — name required/non-blank/≤120, description string|null; no external dep) and `src/boards/boards.routes.ts` (`createBoardsRouter(repo)` exposing `POST /boards`, `GET /boards`, `GET /boards/:id`, `PATCH /boards/:id`, `DELETE /boards/:id` with exact spec status codes; each handler try/catch → `next(err)`; board mutations logged via `log()`). Wired `boardsRepo` into `AppDeps`/`createApp` (`src/app.ts`) alongside a central error-handling middleware (malformed JSON → 400, any other failure → generic 500, `res.headersSent` guard, no stack-trace leak) and constructed `PostgresBoardsRepository` in `src/server.ts`. Added 16 route integration tests (`boards.routes.test.ts`, stateful in-memory stub repo — persistence round-trips, not echoes) + 13 validation unit tests (`boards.validation.test.ts`); updated existing `app.test.ts`/`health.test.ts` to inject a stub `boardsRepo`. All 8 AC groups satisfied (AC-ENTRY-1, AC-HAPPY-1..5, AC-ERROR-1..3). `npm test` 48/48 PASS (29 new + 19 existing, no regression), `npm run build` (tsc, strict) PASS, lint N/A. TypeScript code review: Approve (Warning) — applied `res.headersSent` guard + stack logging. TASK-002 now BUILD_COMPLETE. Committed to `feature/FEAT-002-board-crud`. Next: `/banyan-reflect TASK-002`.
- **2026-07-12** — TASK-002 reflection complete (`/banyan-reflect`). Reflection doc `memory-bank/reflection/reflection-TASK-002.md`. Ratings: **Task Quality High, Ecosystem Effectiveness Good**. Continuous learning: 4 patterns extracted → 2 amended (`testing-patterns`, `error-handling`, both now evidence_count 2) + 2 created (`data-access`, `api-design`); 6 learned rule files total (under cap 10). Top ecosystem improvement flagged: no task-indexed session logs (`.agent-logs/claude/by-task/TASK-002/` absent) so build-session metrics were unavailable. Status → REFLECTION_COMPLETE. Open follow-ups (carried to archive): human live-verify `docker compose up` + Board CRUD curl round-trip; auth/RBAC deferred (tracked gap). Next: `/banyan-archive TASK-002`.

- **2026-07-12** — TASK-003 (Card CRUD API) Phase 1/2 complete: **Data layer**. Added `cards` schema via committed SQL init script `db/init/002_cards.sql` (INTEGER identity PK; `board_id INTEGER NOT NULL REFERENCES boards(id) ON DELETE CASCADE`; `title VARCHAR(200)`; `description TEXT`; `status VARCHAR(20) NOT NULL DEFAULT 'todo' CHECK (status IN ('todo','in_progress','done'))`; `due_date DATE`; `created_at`/`updated_at TIMESTAMPTZ`; `cards_board_id_idx` on `board_id` for the p95<200ms NFR). Numbered `002_` so it runs after `001_boards.sql` (FK target must exist first). Added `src/cards/cards.types.ts` (`Card`, `CreateCardInput`, `UpdateCardInput`, `CardStatus` union — `board_id` absent from update = immutable) and `src/cards/cards.repository.ts` — `CardsRepository` interface (DI, mirrors `BoardsRepository`) + `PostgresCardsRepository` over the existing `pg` pool: parameterized queries (injection-safe), `RETURNING`-based create/update, `findAll(boardId?)` with optional parameterized WHERE filter, app-layer `status` default `'todo'`, dynamic partial `update` bumping `updated_at`, hard `delete` reporting `rowCount`. 14 unit tests in `cards.repository.test.ts` (mocked pool — create defaults, board_id filter, findById null-miss, partial update, delete affected-rows). `npm test` 62/62 PASS (14 new + 48 existing, no regression), `npm run build` (tsc strict) PASS, lint N/A (no lint script). Committed to `feature/FEAT-003-card-crud`. Next: `/banyan-build TASK-003` for Phase 2 (HTTP layer: validation, routes, app/server wiring, board_id-existence check).

- **2026-07-13** — TASK-003 (Card CRUD API) Phase 2/2 complete (final phase): **HTTP layer**. Added `src/cards/cards.validation.ts` (hand-rolled create/update validators — title required/non-blank/≤200, board_id required positive integer, status ∈ enum, description/due_date optional+typed, board_id rejected on update = immutable; no external dep) and `src/cards/cards.routes.ts` (`createCardsRouter(cardsRepo, boardsRepo)` exposing `POST /cards`, `GET /cards` with optional `?board_id=` filter, `GET /cards/:id`, `PATCH /cards/:id`, `DELETE /cards/:id` with exact spec status codes; application-level `board_id`-existence check via `boardsRepo.findById` → `400 { error: 'board_id does not reference an existing board' }` (AC-ERROR-3, no raw FK error leak); each handler try/catch → `next(err)`; card mutations logged via `log()`). Wired `cardsRepo` into `AppDeps`/`createApp` (`src/app.ts`, mounts the cards router with both repos) and constructed `PostgresCardsRepository` in `src/server.ts`; extended `app.test.ts`/`health.test.ts` to inject a stub `cardsRepo`. README updated with the Cards API table and the `002_cards.sql` first-run schema note. Added 15 validation unit tests + 18 route integration tests (`cards.routes.test.ts`, linked in-memory board+card stubs sharing state so the cascade AC-HAPPY-7 is verified at the orchestration layer: `DELETE /boards/:id` drops the board's cards). All AC groups satisfied (AC-ENTRY-1, AC-HAPPY-1..7 incl. cascade, AC-ERROR-1..4). `npm test` 95/95 PASS (33 new + 62 existing, no regression), `npm run build` (tsc strict) PASS, lint N/A. Security: parameterized queries only, no `console.log`, generic 500 with no internal leak, board_id immutable on PATCH. Code review self-approved (mirrors reviewed boards patterns). TASK-003 now BUILD_COMPLETE. Committed to `feature/FEAT-003-card-crud`. Open follow-up: human live-verify `docker compose up` + real-DB `ON DELETE CASCADE`/FK behavior. Next: `/banyan-reflect TASK-003`.

- **2026-07-13** — TASK-003 reflection complete (`/banyan-reflect`). Reflection doc `memory-bank/reflection/reflection-TASK-003.md`. Ratings: **Task Quality High, Ecosystem Effectiveness Good**. Continuous learning: 3 patterns extracted → 3 amended (`api-design` ec→2, `data-access` ec→2, `testing-patterns` ec→3 and **promoted low→medium**); still 6 learned rule files (under cap 10), 0 created. Key learnings: app-level FK-existence check → 400 before insert; optional filter as `findAll(filter?)` + indexed FK; linked shared-store stubs to verify DB cascade at the orchestration layer. Ecosystem caveats: no task-indexed session logs (3rd task); phases built inline rather than via code-reviewer sub-agent (self-review). Status → REFLECTION_COMPLETE. Open follow-ups (carried to archive): human live-DB smoke test (real `ON DELETE CASCADE`/FK/index); extract shared validation-primitives helper before a 3rd CRUD resource; auth/RBAC deferred. Next: `/banyan-archive TASK-003`.

- **2026-07-13** — TASK-004 (React Frontend, FEAT-004) planned via `/banyan-plan` → PLANNING_COMPLETE (Level 3, auto-provisioned for FEAT-004). Spec Writer produced an End-User Feature spec with 11 ACs (1 ENTRY / 6 HAPPY / 3 ERROR / 1 ASYNC), taxonomy PASS-with-warnings (1 soft T-006 on a non-canonical sub-header). 3 build phases planned (scaffold+API client / board list / board view+columns), ~15 component tests (Vitest+RTL). Two gaps surfaced from codebase exploration: API has **no CORS middleware** (`src/app.ts`), and productBrief mentions card **labels** that don't exist in the `cards` schema.

- **2026-07-13** — TASK-004 creative complete (`/banyan-creative`) → CREATIVE_COMPLETE. Two phases: **Architecture Design** (`creative/TASK-004-react-frontend-architecture.md`) — Vite + `@vitejs/plugin-react`, React Router (`/`, `/boards/:id`, `*`), raw `fetch` client with base URL from `import.meta.env.VITE_API_BASE_URL` (default same-origin `/api`), **CORS resolved via Vite dev-server proxy (backend `src/app.ts` untouched)** — proxy also dodges the `/boards` API-vs-route collision; frontend-owned wire types (dates as `string`); Vitest+RTL+jsdom co-located `*.test.tsx`; frontend in `frontend/` at repo root (`"type": "module"`). **UI/UX Design** (`creative/TASK-004-react-frontend-uiux.md`) — board list = semantic `<ul>/<li>/<a>`, board view = CSS Grid 3-col collapsing to single column <640px, shared Loading/Error(+Retry)/Empty/NotFound components, `groupCardsByStatus` guarantees all 3 columns render, WCAG 2.1 AA (status via text label not color-only, `role="alert"`, `aria-live`, `:focus-visible`), plain CSS/CSS Modules (no component lib). Next: `/banyan-build TASK-004` (Phase 1).

- **2026-07-13** — TASK-004 (React Frontend, FEAT-004) Phase 1/3 complete: **Frontend scaffold, tooling & API client**. Created a standalone `frontend/` package at repo root (`"type": "module"`, backend at root `src/` untouched): **Vite 6** + `@vitejs/plugin-react`, **TypeScript** strict, **Vitest 3 + React Testing Library + jsdom** (co-located tests). Added the single I/O seam `src/api/client.ts` — `getBoards()`/`getBoard(id)`/`getCards(boardId)`, the only module that calls `fetch`; base URL read per-call from `import.meta.env.VITE_API_BASE_URL` (12-factor, default same-origin `/api`); returns a discriminated `ApiResult<T>` (`ok` / `http` w/ status incl. 404 / `network`) so later pages can render error vs. not-found; ids `encodeURIComponent`-escaped. Wire types in `src/api/types.ts` (`Board`/`Card`/`CardStatus`, dates as `string`, no `labels`). **CORS resolved with zero backend change** via Vite dev-server proxy (`/api/*` → `VITE_API_PROXY_TARGET` default `http://localhost:3000`, `/api` prefix stripped) — also dodges the `/boards` API-vs-route collision. App shell (`main.tsx`/`App.tsx` placeholder; routing/pages deferred to Phases 2-3). **Verification**: 8/8 Vitest tests PASS (base-URL-from-env ×2, request shaping ×2, result normalization ×4); `tsc --noEmit` clean; `vite build` PASS (bundle 46 kB gzip, <1s budget); dev server boots (VITE ready 312 ms @ :5173); `npm audit` **0 vulnerabilities** (bumped Vitest 2→3 to dedupe onto vite@6 and clear the nested-vite@5/esbuild advisories). Code review (ecc:typescript-reviewer): APPROVE; applied 2 non-blocking fixes. Docs: `techContext.md` frontend `[TBD]`s filled (paths, Vite/Vitest, commands, shared-types decision); `frontend/README.md` added. **Deviation**: no committed `frontend/.env.example` (repo tooling guards `.env*` paths) — the env template lives in `frontend/README.md` instead; both vars have safe defaults so the app runs with no `.env`. **Delivers AC-HAPPY-1.** Committed to `feature/FEAT-004-react-frontend`. Open follow-up: human live-verify `npm run dev` + a real `/api/boards` round-trip against the running API. Next: `/banyan-build TASK-004` for Phase 2 (Board list page).

- **2026-07-13** — TASK-004 (React Frontend, FEAT-004) Phase 3/3 complete (final phase): **Board view + column grouping (`/boards/:id`)**, completing the entry→success journey. `BoardViewPage` fetches `GET /boards/:id` + `GET /cards?board_id=:id` in **parallel** via a local `getBoardView(id)` (composes the client's `getBoard`/`getCards` — keeps the single fetch seam; board 404 propagates as an http/404 result, any other failure as a generic error). One `useApiResource` state machine over both: loading (AC-ASYNC-1) → **distinct 404 "Board not found"** (`NotFoundState`, no Retry, back-link — AC-ERROR-3, separate from the router catch-all) → **error+retry** re-firing both (AC-ERROR-2, no column silently empties) → success. `groupCardsByStatus` pure fn seeds all three keys so **every column always renders** (AC-HAPPY-6) and each card lands in exactly the column matching its `status` (AC-HAPPY-4). Components: `BoardHeader` (back-link + `<h1>`), `Columns` (fixed To Do/In Progress/Done order), `Column` (`<section aria-labelledby>` + `<h2>` "Label (count)" — status via persistent text, never color-only), `Card` (`title`, optional `description`, `due_date` with a "Due:" text label). CSS: 3-col grid collapsing to a single stacked column <640px (UI/UX B1). **Verification**: 20/20 tests PASS (8 client + 6 list + 6 new view, incl. the list→click→columns journey); `tsc --noEmit` clean; `vite build` PASS (69.5 kB gzip); 0 vulns. Also updated the Phase-2 nav test (BoardViewPage is now real, not a placeholder). Code review: orchestrator self-review. **Delivers AC-HAPPY-4, AC-HAPPY-6, AC-ERROR-2, AC-ERROR-3, AC-ASYNC-1 (board)** — TASK-004 now **BUILD_COMPLETE with all 11 ACs satisfied**. Committed to `feature/FEAT-004-react-frontend`. Open follow-up: `/banyan-uat TASK-004` (keyboard traversal, screen-reader column headers/counts, non-color status at each breakpoint) + human live `npm run dev` against the running API. Next: `/banyan-reflect TASK-004`, then `/banyan-archive TASK-004`.

- **2026-07-13** — TASK-004 (React Frontend, FEAT-004) Phase 2/3 complete: **Board list page (`/`)**. Added `react-router-dom` ^6.30.4 and a shared route table `src/routes.tsx` (`/` → BoardListPage, `/boards/:id` → BoardViewPage, `*` → NotFoundState); `App.tsx` now mounts `RouterProvider(createBrowserRouter(routes))`. New `hooks/useApiResource.ts` maps the client's `ApiResult` into a `loading|success|error` state machine with a `reload` for retry (enforces AC-ASYNC-1 "never blank while pending" by construction). Four shared, reused state components (`Loading` w/ `aria-live`, `ErrorState` w/ `role="alert"`+Retry, `EmptyState`, `NotFoundState` w/ back-link and no Retry). `BoardListPage` fetches `GET /boards` and renders one of loading / error+retry / empty ("No boards yet.") / the list; `BoardList`→`BoardListItem` render a semantic `<ul>/<li>/<a>` (UI/UX decision A1) where the whole row is one `<Link to="/boards/:id">` with an `aria-label` combining name+description. `BoardViewPage` is a Phase-3 placeholder so the route resolves and click-through is testable. index.css got list/loading/error styles with preserved `:focus-visible` outlines (WCAG AA). **Verification**: 14/14 tests PASS (8 client + 6 new in `BoardListPage.test.tsx` covering AC-ENTRY-1, AC-HAPPY-2/3/5, AC-ERROR-1, AC-ASYNC-1, + catch-all 404); `tsc --noEmit` clean; `vite build` PASS (bundle 68.9 kB gzip); 0 vulns. Notable fix: the data router (`createMemoryRouter`) builds a fetch `Request` with an `AbortSignal` on navigation that jsdom/undici reject cross-realm, so the loader-free component tests drive the **same** `routes` array through the declarative `MemoryRouter`+`Routes` instead. Code review: orchestrator self-review this phase (patterns mirror the Phase-1 reviewed code; single fetch seam preserved). **Delivers AC-ENTRY-1, AC-HAPPY-2, AC-HAPPY-3, AC-HAPPY-5, AC-ERROR-1, AC-ASYNC-1 (list).** Committed to `feature/FEAT-004-react-frontend`. Next: `/banyan-build TASK-004` for Phase 3 (Board view + column grouping).

- **2026-07-14** — TASK-004 reflection complete (`/banyan-reflect`). Reflection doc `memory-bank/reflection/reflection-TASK-004.md`. Ratings: **Task Quality High, Ecosystem Effectiveness Good**. Recommendation: ready to archive, but `/banyan-uat TASK-004` **strongly recommended first** — this is the project's first UI task and the creative phase's WCAG 2.1 AA claims (keyboard traversal, screen-reader column headers/counts, non-color status per breakpoint) are unverified in a real browser. Continuous learning: 4 patterns extracted → 1 **created** (`frontend-patterns` — single fetch seam + status-union async state, first frontend topic) + 3 **amended** (`testing-patterns` ec→4 query-by-role/accessible-name, +`*.test.tsx`; `configuration` ec→2 align test-runner+bundler majors, +build-tooling scope; `error-handling` ec→3 branch specific-404 before generic error state, **promoted low→medium**). 7 learned files total (under cap 10), 0 systemPatterns changes. Key reflection notes: the backend-scoped learned rules transferred only partially to the first frontend task (DI/12-factor concepts held; SQL/repository rules N/A); real implementation challenges were the Vitest 2→3 dedupe onto Vite 6, the jsdom data-router AbortSignal issue (tests driven through declarative `MemoryRouter`+`Routes`), and the `.env.example` deviation (repo guards `.env*` → template in README). Session logs still not task-indexed (4th consecutive task). Status → REFLECTION_COMPLETE. Open follow-ups (carried to archive): run `/banyan-uat TASK-004`; human live `npm run dev` against the running API. Next: `/banyan-archive TASK-004`.

- **2026-07-13** — TASK-004 **Phase 1 archived** (`/banyan-archive`, Mode A / phase archive) → `memory-bank/archive/archive-TASK-004-phase1.md`. Task remains OPEN (Phases 2-3 pending, reflection not yet run); no merge to main / no PR (deferred to the final Task Archive). Key lessons captured: Vitest/Vite major must share the same Vite major (dedupe check via `npm ls vite`; the 2→3 bump also cleared 5 nested-vite@5 advisories); dev-server proxy > CORS middleware when you own both origins; read env per-call for testable 12-factor config.

---

## Task Archive: TASK-004

**Task**: React Frontend (FEAT-004)
**Status**: ✅ ARCHIVED
**Date**: 2026-07-14
**Archive**: `memory-bank/archive/archive-TASK-004.md`
**Disposition**: local-merge → `main` (Archive Strategy per projectbrief.md). All 3 phases complete; all 11 ACs; 20/20 Vitest+RTL tests; `tsc --noEmit` clean; `vite build` PASS (69.5 kB gzip); 0 vulns. First frontend task — stood up `frontend/` (Vite+React+TS) with zero backend changes (dev-proxy for CORS). Learned-rule consolidation: 7 files (no merges/expiry/pruning); `frontend-patterns` created + `testing-patterns`/`configuration`/`error-handling` amended during reflection (`error-handling` promoted low→medium). Open follow-ups: **`/banyan-uat TASK-004`** (a11y browser walk — first UI, WCAG 2.1 AA claims unverified in a real browser) and human live `npm run dev` against the running API. Note: projectbrief still records `Provider: None` though an `origin` remote now exists; strategy remains local-merge (main not pushed).

---

## Task Archive: TASK-003

**Task**: Card CRUD API (FEAT-003)
**Status**: ✅ ARCHIVED
**Date**: 2026-07-13
**Archive**: `memory-bank/archive/archive-TASK-003.md`
**Disposition**: local-merge → `main` (the active development line; projectbrief's stale `Default Branch: master` corrected to `main` during archive — `master` is an unrelated parallel history with no boards code). All 2 phases complete; 95/95 tests; tsc strict PASS. Learned-rule consolidation: 6 files unchanged (distinct topics), `testing-patterns` promoted low→medium. Open follow-ups: human live-DB smoke test (real `ON DELETE CASCADE`/FK/index); extract shared validation-primitives helper before a 3rd CRUD resource; auth/RBAC deferred.

---

## Task Archive: TASK-001

**Task**: Project Foundation (FEAT-001)
**Status**: ✅ ARCHIVED
**Date**: 2026-07-10
**Archive**: `memory-bank/archive/archive-TASK-001.md`
**Disposition**: local-merge → `master` (no remote). All 3 phases complete; build + tests green; systemPatterns.md backfilled. Learned-rule consolidation: no changes (4 files, distinct topics, all fresh). Open follow-up: human live-verify `docker compose up` (AC-ENTRY-1); deferred npm-audit security debt.

---
